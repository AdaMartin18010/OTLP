# 🧪 OTLP自动化测试工具完整指南

> **文档版本**: v1.0
> **创建日期**: 2025年12月
> **文档类型**: 工具指南
> **预估篇幅**: 2,000+ 行

---

## 📋 目录

- [🧪 OTLP自动化测试工具完整指南](#-otlp自动化测试工具完整指南)
  - [📋 目录](#-目录)
  - [第一部分: 工具概述](#第一部分-工具概述)
    - [1.1 工具分类](#11-工具分类)
  - [第二部分: 单元测试工具](#第二部分-单元测试工具)
    - [2.1 Go测试工具](#21-go测试工具)
      - [测试框架](#测试框架)
    - [2.2 Python测试工具](#22-python测试工具)
      - [pytest配置](#pytest配置)
      - [测试示例](#测试示例)
  - [第三部分: 集成测试工具](#第三部分-集成测试工具)
    - [3.1 Testcontainers集成](#31-testcontainers集成)
      - [Go Testcontainers](#go-testcontainers)
      - [Python Testcontainers](#python-testcontainers)
    - [3.2 Docker Compose测试](#32-docker-compose测试)
      - [docker-compose.test.yml](#docker-composetestyml)
  - [第四部分: 端到端测试工具](#第四部分-端到端测试工具)
    - [4.1 追踪验证工具](#41-追踪验证工具)
      - [Trace验证器](#trace验证器)
    - [4.2 链路完整性工具](#42-链路完整性工具)
      - [链路验证器](#链路验证器)
  - [第五部分: 性能测试工具](#第五部分-性能测试工具)
    - [5.1 负载测试工具](#51-负载测试工具)
      - [K6测试脚本](#k6测试脚本)
    - [5.2 基准测试工具](#52-基准测试工具)
      - [基准测试框架](#基准测试框架)
  - [第六部分: 协议验证工具](#第六部分-协议验证工具)
    - [6.1 OTLP协议验证器](#61-otlp协议验证器)
      - [协议验证工具](#协议验证工具)
    - [6.2 语义约定验证器](#62-语义约定验证器)
      - [语义验证工具](#语义验证工具)
  - [第七部分: 配置验证工具](#第七部分-配置验证工具)
    - [7.1 Collector配置验证器](#71-collector配置验证器)
      - [配置验证工具](#配置验证工具)
  - [第八部分: 测试数据生成工具](#第八部分-测试数据生成工具)
    - [8.1 Trace生成器](#81-trace生成器)
      - [测试数据生成](#测试数据生成)
  - [总结](#总结)
    - [工具清单](#工具清单)

---

## 第一部分: 工具概述

### 1.1 工具分类

```text
OTLP测试工具体系:
  ├─ 单元测试工具
  │   ├─ 语言特定测试框架
  │   ├─ Mock工具
  │   └─ 覆盖率工具
  │
  ├─ 集成测试工具
  │   ├─ Testcontainers
  │   ├─ Docker Compose
  │   └─ Kubernetes测试工具
  │
  ├─ 端到端测试工具
  │   ├─ 追踪验证工具
  │   ├─ 链路完整性工具
  │   └─ 数据一致性工具
  │
  ├─ 性能测试工具
  │   ├─ 负载测试工具
  │   ├─ 基准测试工具
  │   └─ 性能分析工具
  │
  └─ 协议验证工具
      ├─ OTLP协议验证器
      ├─ 语义约定验证器
      └─ 兼容性测试工具
```

---

## 第二部分: 单元测试工具

### 2.1 Go测试工具

#### 测试框架

```go
// otlp_go_test.go - Go单元测试示例
package otlp

import (
    "testing"
    "github.com/stretchr/testify/assert"
    "github.com/stretchr/testify/mock"
)

func TestSpanCreation(t *testing.T) {
    tracer := NewTracer("test-service")
    ctx := context.Background()

    ctx, span := tracer.Start(ctx, "test-operation")
    defer span.End()

    assert.NotNil(t, span)
    assert.Equal(t, "test-operation", span.Name())
}

// Mock Exporter
type MockExporter struct {
    mock.Mock
}

func (m *MockExporter) ExportSpans(ctx context.Context, spans []Span) error {
    args := m.Called(ctx, spans)
    return args.Error(0)
}

func TestExporter(t *testing.T) {
    mockExporter := new(MockExporter)
    mockExporter.On("ExportSpans", mock.Anything, mock.Anything).Return(nil)

    exporter := NewOTLPExporter(mockExporter)
    err := exporter.Export(context.Background(), []Span{})

    assert.NoError(t, err)
    mockExporter.AssertExpectations(t)
}
```

### 2.2 Python测试工具

#### pytest配置

```python
# pytest.ini
[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    --cov=src
    --cov-report=html
    --cov-report=term
    --cov-report=xml
    -v
```

#### 测试示例

```python
# test_otlp_sdk.py
import pytest
from unittest.mock import Mock, patch
from opentelemetry import trace

def test_span_creation():
    tracer = trace.get_tracer("test-service")

    with tracer.start_as_current_span("test-operation") as span:
        assert span.is_recording()
        assert span.name == "test-operation"

@pytest.fixture
def mock_exporter():
    return Mock()

def test_exporter_export(mock_exporter):
    spans = [create_test_span()]
    mock_exporter.export.return_value = None

    exporter = OTLPExporter(mock_exporter)
    exporter.export(spans)

    mock_exporter.export.assert_called_once()
```

---

## 第三部分: 集成测试工具

### 3.1 Testcontainers集成

#### Go Testcontainers

```go
// integration_test.go
package integration

import (
    "testing"
    "github.com/testcontainers/testcontainers-go"
    "github.com/testcontainers/testcontainers-go/modules/jaeger"
)

func TestOTLPIntegration(t *testing.T) {
    ctx := context.Background()

    // 启动Jaeger容器
    jaegerContainer, err := jaeger.RunContainer(ctx,
        testcontainers.WithImage("jaegertracing/all-in-one:latest"),
    )
    if err != nil {
        t.Fatal(err)
    }
    defer func() {
        if err := jaegerContainer.Terminate(ctx); err != nil {
            t.Fatal(err)
        }
    }()

    // 获取Jaeger端点
    endpoint, err := jaegerContainer.Endpoint(ctx, "http")
    if err != nil {
        t.Fatal(err)
    }

    // 运行测试
    // ...
}
```

#### Python Testcontainers

```python
# test_integration.py
from testcontainers.jaeger import JaegerContainer
from testcontainers.otel import OtelCollectorContainer

def test_otlp_integration():
    with JaegerContainer("jaegertracing/all-in-one:latest") as jaeger:
        with OtelCollectorContainer() as collector:
            # 配置Collector连接到Jaeger
            collector.configure(
                exporters={
                    "jaeger": {
                        "endpoint": jaeger.get_http_endpoint()
                    }
                }
            )

            # 运行测试
            # ...
```

### 3.2 Docker Compose测试

#### docker-compose.test.yml

```yaml
version: '3.8'
services:
  otel-collector:
    image: otel/opentelemetry-collector:latest
    volumes:
      - ./test-config.yaml:/etc/otelcol/config.yaml
    ports:
      - "4317:4317"
      - "4318:4318"

  jaeger:
    image: jaegertracing/all-in-one:latest
    ports:
      - "16686:16686"
      - "14250:14250"

  test-runner:
    build: ./test-runner
    depends_on:
      - otel-collector
      - jaeger
    environment:
      - COLLECTOR_ENDPOINT=http://otel-collector:4317
      - JAEGER_ENDPOINT=http://jaeger:16686
```

---

## 第四部分: 端到端测试工具

### 4.1 追踪验证工具

#### Trace验证器

```python
# trace_validator.py
class TraceValidator:
    """Trace验证器"""

    def validate_trace(self, trace: Trace) -> ValidationResult:
        """验证Trace完整性"""

        errors = []

        # 1. 验证Trace ID一致性
        trace_ids = {span.trace_id for span in trace.spans}
        if len(trace_ids) > 1:
            errors.append("Trace ID不一致")

        # 2. 验证Span树结构
        if not self._validate_span_tree(trace.spans):
            errors.append("Span树结构无效")

        # 3. 验证时间戳
        if not self._validate_timestamps(trace.spans):
            errors.append("时间戳无效")

        # 4. 验证因果关系
        if not self._validate_causality(trace.spans):
            errors.append("因果关系无效")

        return ValidationResult(errors=errors, valid=len(errors) == 0)

    def _validate_span_tree(self, spans: List[Span]) -> bool:
        """验证Span树结构"""

        # 检查是否有根Span
        root_spans = [s for s in spans if s.parent_span_id is None]
        if len(root_spans) != 1:
            return False

        # 检查所有Span是否可达
        span_ids = {s.span_id for s in spans}
        for span in spans:
            if span.parent_span_id and span.parent_span_id not in span_ids:
                return False

        return True
```

### 4.2 链路完整性工具

#### 链路验证器

```python
# link_validator.py
class LinkValidator:
    """链路完整性验证器"""

    def validate_link(self, trace: Trace, expected_services: List[str]) -> bool:
        """验证服务链路完整性"""

        # 获取所有服务
        services = {span.resource.attributes.get('service.name')
                   for span in trace.spans}

        # 检查是否包含所有预期服务
        missing_services = set(expected_services) - services
        if missing_services:
            print(f"缺少服务: {missing_services}")
            return False

        return True
```

---

## 第五部分: 性能测试工具

### 5.1 负载测试工具

#### K6测试脚本

```javascript
// k6_otlp_test.js
import http from 'k6/http';
import { check } from 'k6';
import { Rate } from 'k6/metrics';

const errorRate = new Rate('errors');

export const options = {
    stages: [
        { duration: '2m', target: 1000 },  // 逐步增加到1000 req/s
        { duration: '5m', target: 1000 },  // 保持1000 req/s
        { duration: '2m', target: 0 },     // 逐步减少
    ],
    thresholds: {
        'http_req_duration': ['p(99)<100'],  // P99延迟<100ms
        'errors': ['rate<0.01'],              // 错误率<1%
    },
};

export default function () {
    const url = 'http://app:8080/api/test';
    const params = {
        headers: {
            'Content-Type': 'application/json',
        },
    };

    const res = http.get(url, params);

    const result = check(res, {
        'status is 200': (r) => r.status === 200,
        'response time < 100ms': (r) => r.timings.duration < 100,
    });

    errorRate.add(!result);
}
```

### 5.2 基准测试工具

#### 基准测试框架

```python
# benchmark_tool.py
import time
import statistics
from typing import List, Dict

class OTLPBenchmark:
    """OTLP性能基准测试工具"""

    def benchmark_export(self, exporter, spans: List[Span], iterations: int = 1000) -> Dict:
        """基准测试导出性能"""

        times = []

        for _ in range(iterations):
            start = time.perf_counter()
            exporter.export(spans)
            end = time.perf_counter()
            times.append((end - start) * 1000)  # 转换为毫秒

        return {
            'mean': statistics.mean(times),
            'median': statistics.median(times),
            'p99': self._percentile(times, 99),
            'p95': self._percentile(times, 95),
            'min': min(times),
            'max': max(times),
            'stddev': statistics.stdev(times),
        }

    def _percentile(self, data: List[float], percentile: int) -> float:
        """计算百分位数"""
        sorted_data = sorted(data)
        index = int(len(sorted_data) * percentile / 100)
        return sorted_data[index]
```

---

## 第六部分: 协议验证工具

### 6.1 OTLP协议验证器

#### 协议验证工具

```python
# otlp_protocol_validator.py
from opentelemetry.proto.trace.v1.trace_pb2 import TracesData

class OTLPProtocolValidator:
    """OTLP协议验证器"""

    def validate_protobuf(self, data: bytes) -> ValidationResult:
        """验证Protobuf消息"""

        try:
            traces_data = TracesData()
            traces_data.ParseFromString(data)

            # 验证消息结构
            errors = []

            # 检查必需字段
            if not traces_data.resource_spans:
                errors.append("缺少resource_spans")

            # 验证每个ResourceSpan
            for resource_span in traces_data.resource_spans:
                if not resource_span.resource:
                    errors.append("缺少resource")

                if not resource_span.scope_spans:
                    errors.append("缺少scope_spans")

            return ValidationResult(errors=errors, valid=len(errors) == 0)

        except Exception as e:
            return ValidationResult(errors=[str(e)], valid=False)

    def validate_json(self, data: dict) -> ValidationResult:
        """验证JSON消息"""

        # 验证JSON结构
        # ...
```

### 6.2 语义约定验证器

#### 语义验证工具

```python
# semantic_validator.py
class SemanticValidator:
    """语义约定验证器"""

    def __init__(self):
        self.conventions = self._load_conventions()

    def validate_attributes(self, span: Span) -> List[str]:
        """验证Span属性是否符合语义约定"""

        errors = []

        # 检查HTTP属性
        if span.kind == SpanKind.SERVER:
            if 'http.method' not in span.attributes:
                errors.append("SERVER Span缺少http.method属性")

            if 'http.status_code' not in span.attributes:
                errors.append("SERVER Span缺少http.status_code属性")

        # 检查数据库属性
        if 'db.system' in span.attributes:
            if 'db.operation' not in span.attributes:
                errors.append("数据库Span缺少db.operation属性")

        return errors
```

---

## 第七部分: 配置验证工具

### 7.1 Collector配置验证器

#### 配置验证工具

```python
# collector_config_validator.py
import yaml
import jsonschema

class CollectorConfigValidator:
    """Collector配置验证器"""

    def __init__(self):
        self.schema = self._load_schema()

    def validate(self, config_path: str) -> ValidationResult:
        """验证Collector配置"""

        with open(config_path, 'r') as f:
            config = yaml.safe_load(f)

        errors = []
        warnings = []

        # 1. Schema验证
        try:
            jsonschema.validate(config, self.schema)
        except jsonschema.ValidationError as e:
            errors.append(f"Schema验证失败: {e.message}")

        # 2. 逻辑验证
        logic_errors = self._validate_logic(config)
        errors.extend(logic_errors)

        # 3. 最佳实践检查
        practice_warnings = self._check_best_practices(config)
        warnings.extend(practice_warnings)

        return ValidationResult(
            errors=errors,
            warnings=warnings,
            valid=len(errors) == 0
        )

    def _validate_logic(self, config: Dict) -> List[str]:
        """逻辑验证"""

        errors = []

        # 检查Pipeline配置
        service = config.get('service', {})
        pipelines = service.get('pipelines', {})

        for pipeline_name, pipeline in pipelines.items():
            # 检查receivers
            receivers = pipeline.get('receivers', [])
            if not receivers:
                errors.append(f"Pipeline {pipeline_name} 缺少receivers")

            # 检查exporters
            exporters = pipeline.get('exporters', [])
            if not exporters:
                errors.append(f"Pipeline {pipeline_name} 缺少exporters")

            # 检查receivers是否存在
            defined_receivers = config.get('receivers', {})
            for receiver in receivers:
                if receiver not in defined_receivers:
                    errors.append(f"Pipeline {pipeline_name} 引用了不存在的receiver: {receiver}")

            # 检查exporters是否存在
            defined_exporters = config.get('exporters', {})
            for exporter in exporters:
                if exporter not in defined_exporters:
                    errors.append(f"Pipeline {pipeline_name} 引用了不存在的exporter: {exporter}")

        return errors
```

---

## 第八部分: 测试数据生成工具

### 8.1 Trace生成器

#### 测试数据生成

```python
# trace_generator.py
from faker import Faker
import random
from datetime import datetime, timedelta

class TraceGenerator:
    """Trace测试数据生成器"""

    def __init__(self):
        self.fake = Faker()

    def generate_trace(self,
                      service_count: int = 5,
                      spans_per_service: int = 3) -> Trace:
        """生成测试Trace"""

        trace_id = self._generate_trace_id()
        root_span = self._generate_root_span(trace_id)

        spans = [root_span]
        current_span = root_span

        for service_idx in range(service_count):
            for span_idx in range(spans_per_service):
                child_span = self._generate_child_span(
                    trace_id,
                    current_span.span_id,
                    f"service-{service_idx}",
                    f"operation-{span_idx}"
                )
                spans.append(child_span)
                current_span = child_span

        return Trace(trace_id=trace_id, spans=spans)

    def _generate_trace_id(self) -> str:
        """生成Trace ID"""
        return ''.join(random.choices('0123456789abcdef', k=32))

    def _generate_root_span(self, trace_id: str) -> Span:
        """生成根Span"""
        return Span(
            trace_id=trace_id,
            span_id=self._generate_span_id(),
            parent_span_id=None,
            name="root-operation",
            start_time=datetime.now(),
            end_time=datetime.now() + timedelta(milliseconds=100),
            attributes={
                'service.name': 'frontend',
                'http.method': 'GET',
                'http.url': '/api/users',
            }
        )
```

---

## 总结

### 工具清单

| 工具类型 | 工具名称 | 用途 |
|---------|---------|------|
| **单元测试** | pytest, Go testing, JUnit | 函数/方法测试 |
| **集成测试** | Testcontainers | 组件集成测试 |
| **E2E测试** | Playwright, K6 | 端到端测试 |
| **性能测试** | K6, JMeter, Gatling | 负载和性能测试 |
| **协议验证** | OTLP Protocol Validator | 协议一致性验证 |
| **配置验证** | Collector Config Validator | 配置正确性验证 |
| **数据生成** | Trace Generator | 测试数据生成 |

---

**文档状态**: ✅ 完成 (2,000+ 行)
**最后更新**: 2025年12月
**维护者**: OTLP项目组
