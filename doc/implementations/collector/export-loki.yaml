receivers:
  otlp:
    protocols:
      grpc: {}

exporters:
  loki:
    endpoint: http://localhost:3100/loki/api/v1/push

service:
  pipelines:
    logs:
      receivers: [otlp]
      exporters: [loki]

